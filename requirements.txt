# Core simulation
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Sobol sensitivity analysis (used when RUN_SOBOL = True)
SALib>=1.4.7

# Optional landscape preprocessing (used when BUILD_LANDSCAPE = True)
geopandas>=0.14
planetary-computer>=1.0
pystac-client>=0.7
rasterio>=1.3
shapely>=2.0
